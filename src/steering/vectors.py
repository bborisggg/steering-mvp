"""Steering directions and the frozen TRAIN/DEV/TEST split.

SAE decoder rows and persona vectors, the usability filter, split construction, fingerprints,
and the leakage checks that hard-fail on any intersection.
"""
